{
  "name": "minigame",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "npm run build && npm run serve:lite",
    "serve:lite": "lite-server --baseDir=dist"
  },
  "devDependencies": {
    "lite-server": "^2.6.1",
    "typescript": "^5.4.0",
    "vite": "^5.1.0"
  }
}
